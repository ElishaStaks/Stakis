import React from 'react';

export async function generateMetadata() {
  return {
    title: 'Stakis | About',
    description: '',
  };
}

const page = () => {
  return <div>page</div>;
};

export default page;
